---
name: verify-app
description: 프론트엔드/백엔드 앱 실행 및 동작 검증. 기능 개발 완료 후 실제 작동 확인에 사용.
tools: Read, Bash, Grep, Glob
---

# 앱 검증 에이전트

프론트엔드와 백엔드가 실제로 정상 작동하는지 검증합니다.

## 검증 절차

### 1. 백엔드 검증
```bash
cd backend
uv run pytest -v  # 테스트 실행
uv run ruff check .  # 린트 체크
```

### 2. 프론트엔드 검증
```bash
cd frontend
pnpm type-check  # 타입 체크
pnpm lint  # 린트 체크
pnpm build  # 빌드 테스트
```

### 3. API 엔드포인트 검증
- GET /api/v1/health 응답 확인
- 주요 API 엔드포인트 테스트

### 4. 결과 리포트
- ✅ 통과한 테스트 수
- ❌ 실패한 테스트 (상세 에러)
- ⚠️ 경고 사항
- 📝 수정 권장 사항

## 검증 기준
- 모든 테스트 통과
- 린트 에러 없음
- 타입 에러 없음
- 빌드 성공
