[project]
name = "youtube-transcript"
version = "0.1.0"
description = "유튜브 영상 자막/대본 추출"
requires-python = ">=3.10"
dependencies = [
    "youtube-transcript-api>=1.0.0",
]

[project.scripts]
yt-transcript = "main:main"
