# 시스템 아키텍처

> AI Agent Platform 전체 시스템 구조

---

## 📐 전체 구조

```
┌─────────────────────────────────────────────────────────┐
│                      클라이언트                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │   웹 브라우저  │  │  모바일 앱   │  │   API 클라이언트 │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                     프론트엔드                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │              Next.js 14 (App Router)             │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐        │   │
│  │  │  Pages   │ │Components│ │  Stores  │        │   │
│  │  └──────────┘ └──────────┘ └──────────┘        │   │
│  └─────────────────────────────────────────────────┘   │
│                    Railway (배포)                       │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                      백엔드                              │
│  ┌─────────────────────────────────────────────────┐   │
│  │                   FastAPI                        │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐        │   │
│  │  │   API    │ │ Services │ │  Models  │        │   │
│  │  │ Routers  │ │ (Logic)  │ │(Pydantic)│        │   │
│  │  └──────────┘ └──────────┘ └──────────┘        │   │
│  └─────────────────────────────────────────────────┘   │
│                    Railway (배포)                       │
└─────────────────────────────────────────────────────────┘
                          │
            ┌─────────────┼─────────────┐
            ▼             ▼             ▼
┌───────────────┐ ┌───────────────┐ ┌───────────────┐
│   Supabase    │ │  Claude API   │ │  외부 서비스   │
│  (PostgreSQL) │ │  (Anthropic)  │ │  (MCP 연동)   │
└───────────────┘ └───────────────┘ └───────────────┘
```

---

## 🗂️ 디렉토리 구조

```
ai-agent-platform/
├── frontend/                 # Next.js 프론트엔드
│   ├── src/
│   │   ├── app/             # App Router 페이지
│   │   ├── components/      # 컴포넌트
│   │   │   ├── ui/         # 공통 UI
│   │   │   └── features/   # 기능별 컴포넌트
│   │   ├── hooks/          # 커스텀 훅
│   │   ├── lib/            # 유틸리티
│   │   ├── stores/         # Zustand 상태
│   │   └── types/          # TypeScript 타입
│   └── public/              # 정적 파일
│
├── backend/                  # FastAPI 백엔드
│   ├── app/
│   │   ├── api/v1/         # API 라우터
│   │   ├── core/           # 설정, 보안
│   │   ├── models/         # Pydantic 모델
│   │   ├── services/       # 비즈니스 로직
│   │   │   └── agents/    # 에이전트 서비스
│   │   └── utils/          # 유틸리티
│   ├── tests/               # 테스트
│   └── main.py              # 진입점
│
├── supabase/                 # 데이터베이스
│   └── migrations/          # 마이그레이션
│
├── .claude/                  # Claude Code 설정
│   ├── skills/              # 개발 패턴
│   ├── agents/              # 서브에이전트
│   ├── commands/            # 커스텀 명령어
│   └── settings.json        # 설정
│
└── docs/                     # 문서
```

---

## 🔄 데이터 흐름

### 에이전트 실행 흐름

```
1. 사용자 → 프론트엔드 (메시지 입력)
              │
2.            ▼
   프론트엔드 → 백엔드 (POST /api/v1/agents/{id}/execute)
              │
3.            ▼
   백엔드 → Claude API (스트리밍 요청)
              │
4.            ▼
   Claude API → 백엔드 (SSE 스트림)
              │
5.            ▼
   백엔드 → 프론트엔드 (SSE 전달)
              │
6.            ▼
   프론트엔드 → 화면 (실시간 렌더링)
```

---

## 🗄️ 데이터베이스 스키마

### ai_agents
| 컬럼 | 타입 | 설명 |
|-----|------|------|
| id | UUID | PK |
| name | VARCHAR | 에이전트 이름 |
| description | TEXT | 설명 |
| system_prompt | TEXT | 시스템 프롬프트 |
| model | VARCHAR | 모델 ID |
| temperature | FLOAT | 온도 (0-1) |
| user_id | UUID | FK → users |
| created_at | TIMESTAMP | 생성일 |
| updated_at | TIMESTAMP | 수정일 |

### agent_executions
| 컬럼 | 타입 | 설명 |
|-----|------|------|
| id | UUID | PK |
| agent_id | UUID | FK → ai_agents |
| input | TEXT | 입력 메시지 |
| output | TEXT | 출력 메시지 |
| tokens_used | INT | 사용 토큰 |
| created_at | TIMESTAMP | 실행일 |

---

## 🔌 외부 연동

### MCP 서버 (10개)
| 서버 | 용도 |
|-----|------|
| github | GitHub API 연동 |
| postgres | DB 직접 쿼리 |
| slack | Slack 메시지 |
| obsidian | 노트 읽기/쓰기 |
| fetch | 웹 페이지 가져오기 |
| puppeteer | 브라우저 자동화 |
| memory | 메모리 저장 |
| brave-search | 웹 검색 |
| filesystem | 파일 시스템 |
| context7 | 컨텍스트 관리 |

---

## 🚀 배포 구조

```
┌─────────────┐     ┌─────────────┐
│   Railway   │     │   Railway   │
│  Frontend   │────▶│   Backend   │
│  (Next.js)  │     │  (FastAPI)  │
└─────────────┘     └─────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │  Supabase   │
                    │  (Cloud)    │
                    └─────────────┘
```

| 서비스 | URL |
|--------|-----|
| Frontend | agentplat-production-cf45.up.railway.app |
| Backend | agentplat-production.up.railway.app |
